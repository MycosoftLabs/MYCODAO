# Copy to /opt/mycodao/deploy.env on VM 198 (chmod 600). Never commit real values.
#
# Used by scripts/blue-green-deploy.sh for Cloudflare cache purge after cutover.
# Load website .credentials.local locally, then run:
#   python scripts/_install_deploy_env.py

CF_ZONE_ID=
CF_API_TOKEN=
CLOUDFLARE_ZONE_ID=
CLOUDFLARE_API_TOKEN=
PUBLIC_HOST=blocks.mycodao.com
